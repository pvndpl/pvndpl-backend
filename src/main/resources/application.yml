spring:
  datasource:
    url: ${DB_URL}
    password: ${DB_USERNAME}
    username: ${DB_PASSWORD}

springdoc:
  show-actuator: false
  swagger-ui:
    enabled: true
    path: /api

jwt.secret: ${SEC_SECRET}
server:
  servlet:
    context-path: /api